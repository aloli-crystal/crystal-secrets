= secrets
:toc: left
:toclevels: 2
:source-highlighter: rouge

image:https://github.com/aloli-crystal/secrets/actions/workflows/ci.yml/badge.svg[CI,link=https://github.com/aloli-crystal/secrets/actions/workflows/ci.yml]

Gestion des secrets pour l'écosystème Crystal Aloli : un coffre
TOML chiffré avec `age`, une clé maître stockée dans le Keychain
macOS (synchronisée entre Macs via iCloud), un code de recovery
papier protégé par une passphrase Diceware en filet de sécurité.

Version anglaise : link:README.adoc[README.adoc].

== État : v0.2 (mono-utilisateur macOS, coffre avec préservation des commentaires)

Ce shard est la base du système de secrets Aloli décrit dans
`prod-crystal/CRYSTAL-SECRETS-SPECS.adoc`. La version actuelle
couvre la boucle quotidienne de l'opérateur sur son Mac :

* `init` génère une paire de clés `age`, propose une passphrase
  Diceware via `aloli-crystal/diceware`, imprime la clé
  publique et le code de recovery papier.
* `vault create / get / set / list` pour l'accès quotidien, bâti
  sur `TOML::Document` de `aloli-crystal/toml` — les
  commentaires et la mise en forme survivent aux modifications
  programmatiques.
* `master-key export / import` pour le flux de recovery papier.

Hors périmètre (à venir) :

* **Coffres multi-recipients** (partage équipe). Recipient unique
  pour l'instant.
* **Mode agent SSH** (le coffre `ssh-keys.age` + l'agent en
  mémoire qui sert le protocole ssh-agent v2).
* **Backends d'autres OS** (Secret Service Linux, Credential
  Manager Windows, fichier+passphrase FreeBSD). Keychain macOS
  uniquement.
* Sous-commandes **rotation, delete, edit, diff/log**.

== Architecture

[source]
----
                  ┌──────────────────────────────────────┐
                  │            secrets           │
                  │                                      │
                  │  CLI                                 │
                  │   │                                  │
                  │   ▼                                  │
                  │  MasterKey ─── KeychainMacOS ◄───────┼─── /usr/bin/security
                  │   │              (shell-out)         │
                  │   ▼                                  │
                  │  Vault ──────── age ◄────────────────┼─── /usr/local/bin/age
                  │   │              (shell-out)         │
                  │   ▼                                  │
                  │  TOML::Document ◄─── aloli-crystal/  │
                  │  (AST riche,         toml            │
                  │   commentaires préservés)            │
                  │                                      │
                  │  Diceware     ◄─── aloli-crystal/    │
                  │                    diceware          │
                  │                                      │
                  │  Recovery ──── openssl enc ◄─────────┼─── /usr/bin/openssl
                  │                (passphrase)          │
                  │                                      │
                  └──────────────────────────────────────┘
----

== Installation

[source,shell]
----
brew install age              # dépendance runtime requise
shards install
crystal build src/cli.cr -o secrets --release
sudo install -m 0755 secrets /usr/local/bin/
----

== Démarrage rapide

[source,shell]
----
$ secrets init
Generated Diceware passphrase (7 words):

    perplex grouchy abdomen catacomb mournful prancing slingshot

Accept this passphrase? [O/r/n] o

══════════════════════════════════════════════════════════
  PUBLIC KEY (add this to your team recipients.txt) :

    age1abc123...

══════════════════════════════════════════════════════════
  PAPER RECOVERY CODE — print this and store in a physical safe.
══════════════════════════════════════════════════════════

  Passphrase :
      perplex grouchy abdomen catacomb mournful prancing slingshot

-----BEGIN CRYSTAL-SECRETS RECOVERY-----
U2FsdGVkX1+abc...
-----END CRYSTAL-SECRETS RECOVERY-----

$ secrets vault create -n prod
vault created: ~/.config/secrets/vaults/prod.toml.age

$ secrets set -n prod -k DATABASE_URL
Value (hidden): ●●●●●●●●●●●●
set DATABASE_URL in prod

$ secrets get prod DATABASE_URL
postgres://aloli:secret@db/prod

$ secrets list prod
DATABASE_URL
----

== Récupération (après perte du Mac)

[source,shell]
----
# Sur le nouveau Mac, si la restauration iCloud n'est pas dispo :
$ secrets master-key import
Paste the recovery paper, then Ctrl-D :
-----BEGIN CRYSTAL-SECRETS RECOVERY-----
U2FsdGVkX1+abc...
-----END CRYSTAL-SECRETS RECOVERY-----
^D
Passphrase (hidden): ●●●●●●●●●●●
master key restored. Public key:
    age1abc123...
----

En pratique, **iCloud Keychain** fait le travail automatiquement :
la clé maître suit l'utilisateur sur le nouveau Mac sans qu'il
ait à retaper quoi que ce soit. Le papier est le filet de sécurité
ultime, utilisé uniquement si iCloud lui-même est indisponible.

== Discipline de la clé en mémoire

* La clé maître n'est **jamais** passée en argument de ligne de
  commande (visible via `ps`).
* Les valeurs des secrets ne sont **jamais** passées via argv non
  plus ; `set` lit la valeur depuis stdin (cachée quand STDIN est
  un terminal).
* La clé privée age est passée à `age(1)` via un fichier temp en
  mode `0600`, supprimé dès le retour d'`age`.
* Sur disque, le coffre est `<env>.toml.age` (payload TOML chiffré
  par age). La clé maître ne touche pas le disque sauf dans le
  blob Keychain (géré par macOS).
* Le papier de recovery = blob chiffré openssl ; la passphrase est
  l'unique protection du papier. Ranger les deux dans le même
  coffre physique pour ne pas dupliquer le risque.

== Développement

[source,shell]
----
shards install
crystal spec
crystal tool format --check
bin/ameba
----

19 tests couvrent Keychain shell-out (clé jamais en argv,
8 cas), Vault round-trip (vrai `age`, 6 cas), Recovery
export/import (5 cas). Le comportement Diceware et TOML est
testé par les suites des shards amont.

== Licence

MIT — voir link:LICENSE[LICENSE].

== Références

* https://age-encryption.org/v1[Spécification age v1]
* https://github.com/aloli-crystal/toml[toml] — parseur/sérialiseur TOML v1.0 avec préservation des commentaires
* https://github.com/aloli-crystal/diceware[diceware] — générateur de passphrases Diceware
* https://www.eff.org/dice[Liste EFF Diceware]
* https://github.com/mbelivo/diceware-wordlists-fr[Liste française mbelivo]
* `prod-crystal/CRYSTAL-SECRETS-SPECS.adoc` — spec parente
